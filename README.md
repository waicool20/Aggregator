This project aggregates multiple anime fansub torrent sources and downloads the .torrent files into a folder, works with RSS feeds that only provide magnet links.

# Running it
```
# Windows
./gradlew.bat run

# Unix
./gradlew run
```
